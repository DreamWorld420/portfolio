import type { FC } from "react";

const Hero: FC = () => {
	return (
		<div id="hero">
			<h1>
				I&apos;m Kasra - a frontend web developer, maker of digital tool
				and toys.
			</h1>
		</div>
	);
};

export default Hero;
